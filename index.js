
const names = ["Guadalupe", "Ollie", "Aki"]
len = names.length;
function writeCards(names){
for (let c = 0;c < len; c++){
    console.log(`thank you ${names[c]} for the bitch ass gift`);
    debugger;
}
}
writeCards(names);


let countdown = 10;
while (countdown > -1 ){
    console.log(countdown--);

}